DESCRIPTION
-----------
upstream:
A pure copy of the 2.2.0.1 version of SWFUpload

UPDATING
--------
$ sh refresh_upstream.sh
$ git commit -m "Updated SwfUpload to <VERSION_NUMBER>" upstream
$ git push

$ cp upstream/{swfupload.js,Flash/swfupload.swf,plugins/swfupload.queue.js} ../../gallery3/lib/swfupload
$ cd ../../gallery3
$ git commit -m "Updated to Exifer r<REVISION_NUMBER>" lib/swfupload
$ git push
